# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment (contains secrets)
.env

# Dependencies
node_modules/
# Note: package-lock.json files ARE tracked for reproducible builds
# Each app (api, web-client) has its own lock file

# Build artifacts
dist/
build/
*.tsbuildinfo


# Original raw data (not processed)
original_data/
backups/
